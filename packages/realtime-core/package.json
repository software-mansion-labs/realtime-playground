{
  "name": "@realtime-playground/realtime-core",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "eslint": "yarn run -T eslint .",
    "prettier": "yarn run -T prettier --check . --ignore-path ../../.prettierignore",
    "format": "yarn prettier",
    "ts": "yarn run -T tsc --project tsconfig.json --noEmit",
    "lint": "yarn eslint && yarn format && yarn ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "*",
    "zod": "*",
    "zustand": "*"
  },
  "peerDependencies": {
    "react": "*"
  }
}
